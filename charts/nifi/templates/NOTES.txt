This chart is the initial NiFi 2.x scaffold for nifi2-platform.

- Standalone mode is the default.
- Set controllerManaged.enabled=true only when using the optional controller.
- The StatefulSet template and nifi.properties content are intentionally minimal in this scaffold.
- Create the TLS Secret referenced by .Values.tls.existingSecret before installing into a secure cluster.

