apiVersion: apps/v1
kind: Deployment
metadata:
  name: nifi-fabric-controller-manager
  namespace: nifi-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nifi-fabric-controller-manager
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nifi-fabric-controller-manager
    spec:
      serviceAccountName: nifi-fabric-controller-manager
      containers:
      - name: manager
        image: nifi-fabric-controller:dev
        imagePullPolicy: Never
        args:
        - --leader-elect
        ports:
        - name: metrics
          containerPort: 8080
        - name: probes
          containerPort: 8081
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
