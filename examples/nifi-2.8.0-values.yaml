# Focused compatibility overlay for the newer NiFi 2.x proof path.
# Compose with either examples/standalone/values.yaml or examples/managed/values.yaml.
# Keep this at 2 replicas on single-node kind so the proof stays lightweight and repeatable.
replicaCount: 2

image:
  repository: apache/nifi
  tag: "2.8.0"
  pullPolicy: IfNotPresent
